<% headers = ['title', 'Content', "Slug", "User_id", "Level_name", "Material_name", "Status", "Created_at"] %>
<%= CSV.generate_line headers %>
<% @course_items.each do |item| %>
    <%= CSV.generate_line([item.title, item.content, item.slug, item.user_id, item.level_name, item.material_name, item.status, item.created_at]) -%>
<% end %>

